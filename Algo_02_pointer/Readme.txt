Algorithm : Algo 02 pointer

A variable called 'free_end' is used as pointer 
to separate graph that cannot satisfy the size condition.
Accordingly deletion is also done.

Rest is same as algo 01 vector